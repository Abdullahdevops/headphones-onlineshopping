export interface Cart {
  productKey?: string;
  productName?: string;
  productPrice?: number;
  productQuantity?: number;
  productPhoto?: HTMLImageElement;
  total?: number;
  userEmail?: string;
}
